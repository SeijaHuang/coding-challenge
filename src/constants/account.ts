export enum EACCOUNT_CATEGORY {
  REVENUE = "revenue",
  EXPENSE = "expense",
  ASSETS = "assets",
  LIABILITY = "liability",
}

export enum EACCOUNT_TYPE {
  SALES = "sales",
  OVERHEADS = "overheads",
  BANK = "bank",
  FIXED = "fixed",
  PAYROLL = "payroll",
  TAX = "tax",
  CURRENT = "current",
  CURRENT_ACCOUNTS_PAYABLE = "current_accounts_payable",
  CURRENT_ACCOUNTS_RECEIVABLE = "current_accounts_receivable",
}

export enum EVALUE_TYPE {
  CREDIT = "credit",
  DEBIT = "debit",
}
